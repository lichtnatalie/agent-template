# Real config — never commit, contains live secrets
openclaw.json

# Runtime state / secrets
credentials/
logs/
telegram/
sessions/
# Memory contents are personal — keep the directories, ignore what fills them
memory/*
workspace/**/memory/*
!workspace/**/memory/.gitkeep

# Your real, filled-in USER.md (keep only the placeholder version in this repo)
workspace/USER.md.real

# OS junk
.DS_Store
